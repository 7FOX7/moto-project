import { StyledButton } from "../../styles/style"

const CustomButton = ({content}) => {
   return (
      <>
         <StyledButton>
            {content}
         </StyledButton>
      </>
   ) 
}

export default CustomButton